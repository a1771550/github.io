@using UI.Helpers
@model UI.Models.WcAdminViewModel
@{
	Layout = "~/Views/Shared/_Layout.cshtml";
	ViewBag.Title = "Index";
}


<ul id="letters">
	@foreach (var letter in Model.Letters)
	{
		<li>@Html.ActionLink(letter, "Index", "Admin", new { letter = letter }, null)</li>
	}
</ul>
<hr />

<div id="listDiv" class="marginTop1em alignLeft">
	@*@foreach (var collocation in Model.CollocationViewModels)
	{
		@Html.Partial("CollocationList", collocation)
	}*@
	@foreach (var collocation in Model.CollocationListInGroup)
	{
		@Html.Partial("WordList",collocation)
	}
</div>

<!--
	<div class="pagination">
  <ul>
    <li><a href="#">«</a></li>
    <li class="active"><a href="#">1</a></li>
    <li><a href="#">2</a></li>
    <li><a href="#">»</a></li>
  </ul>
</div>
	-->

<div class="pagination">
	@Html.PageLinks(Model.PagingInfo, x => Url.Action("Index", new { page = x }))
</div>